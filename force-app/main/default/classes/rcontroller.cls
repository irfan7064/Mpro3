public with sharing class rcontroller {
   
    @AuraEnabled
    public static string retmethod(){
        map<integer,String> kmap=new mapmap<integer,String>();
        kmap.put()
        try {
            
        } catch (Exception e) {
            throw new AuraHandledException(e.getMessage());
        }
    }
   
}
